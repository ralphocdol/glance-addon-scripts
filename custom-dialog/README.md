[⇐ addon-script list](../#addon-scripts)

Just a basic dialog box, this will be an optional dependency for other addon script since there's already a native `confirm`. Only useful if you prefer the layout to at least match the styling.

![preview](preview/preview1.png)